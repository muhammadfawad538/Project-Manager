# pmagent — Production CrewAI agents package.
#
# Keep this file minimal: heavy imports are deferred into sub-modules so that
# `import pmagent` stays fast and side-effect free.
# Use the fully qualified form for every symbol: `from pmagent.db import ...`
